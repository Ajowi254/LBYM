{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "type": "object",
    "required": ["categoryId", "budgetLimit"],
    "properties": {
      "categoryId": {
        "type": "integer",
        "description": "The unique identifier of the category."
      },
      "budgetLimit": {
        "type": "number",
        "description": "The budget limit set by the user for the category.",
        "minimum": 0
      }
    }
  }
  